# 识图能力（deepseek_eyes）

底层模型（如 DeepSeek）不具备原生识图能力。遇到图片时，**不要用 Read 工具去读图片文件**，也不要猜测图片内容，统一调用 MCP 工具 `describe_image` 或 `describe_clipboard_image` 完成识图。

## 触发场景

- 用户在输入框**直接粘贴图片**（如果客户端不支持把图片传给模型，对话里会显示 `[Unsupported Image]` 占位符，此时拿不到图片路径）
- 消息中出现 `Saved attachments:` / `Attached image:` 并列出图片路径
- 用户要求「识别 / 描述 / 分析 / 看这张图」，并给出了图片路径或 URL
- 用户把某张图片文件拖进终端，或在对话里提到某张截图/照片

## 使用方法（按优先级）

### 1. 剪贴板识图（用户粘贴单张图片但模型看不到图时，最优先）

用户粘贴图片的动作会把图片写进系统剪贴板。直接调用：

```
describe_clipboard_image(prompt="<用户的意图>")
```

- 适用：对话里出现 `[Unsupported Image]`、`<image>` 等占位符，模型没有收到真实图片路径
- **不需要传路径**，工具会自动读取系统剪贴板里的图片
- 如果返回「剪贴板中未找到图片」，再提醒用户重新复制/粘贴一次图片

### 2. 批量识图（一次处理多张图片时）

剪贴板只会保留最后一张图片，多张图片请让用户放进一个文件夹，再调用：

```
describe_images_in_folder(folder="<文件夹路径>", prompt="<对每张图的要求>", recursive=False, limit=10)
```

- 会自动扫描文件夹里所有图片，逐张识别并返回每张图各自的描述
- `recursive=True` 可递归子文件夹；`limit` 限制最多识别张数（默认 10）
- 适合场景：用户说「看一下这几张截图」「对比下面几张图」，引导他把图片放到一个文件夹

### 3. 按路径识图（有明确路径时）

把图片来源传给 `describe_image` 的 `image` 参数：

```
describe_image(image="<图片路径或URL>", prompt="<用户的意图，默认详细描述>")
```

- 本地图片：传绝对路径，例如 `C:\Users\ming\Pictures\截图.png`
- 粘贴的图片：传 `Saved attachments:` 中列出的路径
- 网络图片：直接传 `http(s)://` 链接（`describe_image` 会自动识别 URL）

## 注意

- 保持图片原样传递，不要用 Read 工具读图片内容（模型无视觉，读不出东西）
- 识别结果返回的是文字描述，可在此基础上继续回答用户的后续问题
- 若用户没有特别要求语言，用中文回答
